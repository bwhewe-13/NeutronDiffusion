#pragma once

#include <ndiffusion/types.hpp>

/**
 * @file solver_3d.hpp
 * @brief 3-D multigroup neutron diffusion solver declarations (future).
 *
 * Placeholder for future 3-D (x-y-z, r-theta-z) solver implementations.
 * Will reuse the shared types defined in types.hpp.
 */

// TODO: Define Mesh3D struct and DiffusionSolver3D / TimeDependentSolver3D classes.
